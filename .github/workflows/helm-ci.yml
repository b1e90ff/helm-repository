name: Helm Charts CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ '*' ]

jobs:
  find-charts:
    runs-on: ubuntu-latest
    outputs:
      charts: ${{ steps.find-charts.outputs.charts }}
      matrix: ${{ steps.find-charts.outputs.matrix }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        fetch-depth: 0

    - name: Find Helm charts
      id: find-charts
      run: |
        charts=$(find . -name "Chart.yaml" -not -path "./.git/*" | xargs -I {} dirname {} | tr '\n' ' ')
        echo "charts=$charts" >> $GITHUB_OUTPUT
        echo "Found charts: $charts"
        
        # Create matrix for parallel processing
        matrix_json=$(echo "$charts" | tr ' ' '\n' | grep -v '^$' | sed 's/^/"/;s/$/"/' | paste -sd, - | sed 's/^/[/;s/$/]/')
        echo "matrix=$matrix_json" >> $GITHUB_OUTPUT
        echo "Matrix: $matrix_json"

  test-charts:
    runs-on: ubuntu-latest
    needs: find-charts
    if: needs.find-charts.outputs.matrix != '[]'
    strategy:
      fail-fast: false
      matrix:
        chart: ${{ fromJson(needs.find-charts.outputs.matrix) }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Helm
      uses: azure/setup-helm@v4.3.1
      with:
        version: '3.14.0'

    - name: Helm registry login
      run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build Helm dependencies
      run: |
        echo "🔄 Building Helm dependencies for ${{ matrix.chart }}"
        helm dependency build "${{ matrix.chart }}"
        echo "✅ Dependencies built"

    - name: Lint Helm chart
      run: |
        echo "🔍 Linting chart: ${{ matrix.chart }}"
        if ! helm lint "${{ matrix.chart }}"; then
          echo "❌ Linting failed for chart: ${{ matrix.chart }}"
          exit 1
        fi
        echo "✅ Linting passed for chart: ${{ matrix.chart }}"

    - name: Validate Chart.yaml
      run: |
        echo "🔍 Validating Chart.yaml for: ${{ matrix.chart }}"
        
        if ! grep -q "^name:" "${{ matrix.chart }}/Chart.yaml"; then
          echo "❌ Error: Chart.yaml missing 'name' field"
          exit 1
        fi
        
        if ! grep -q "^version:" "${{ matrix.chart }}/Chart.yaml"; then
          echo "❌ Error: Chart.yaml missing 'version' field"
          exit 1
        fi
        
        version=$(grep "^version:" "${{ matrix.chart }}/Chart.yaml" | cut -d' ' -f2 | tr -d '"')
        if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          echo "❌ Error: Invalid version format: $version (must be semver like 1.0.0)"
          exit 1
        fi
        
        echo "✅ Chart.yaml validation passed"

    - name: Template rendering tests
      run: |
        chart_name=$(basename "${{ matrix.chart }}")
        echo "🧪 Testing template rendering for chart: ${{ matrix.chart }}"
        
        # Test default values
        if ! helm template "test-$chart_name" "${{ matrix.chart }}" --namespace test > /dev/null; then
          echo "❌ Template rendering failed with default values"
          exit 1
        fi
        
        # Test different namespaces and release names
        for ns in "default" "production" "monitoring"; do
          if ! helm template "test-$chart_name" "${{ matrix.chart }}" --namespace "$ns" > /dev/null; then
            echo "❌ Template rendering failed with namespace '$ns'"
            exit 1
          fi
        done
        
        for release in "short" "very-long-release-name-that-might-cause-issues"; do
          if ! helm template "$release" "${{ matrix.chart }}" --namespace test > /dev/null; then
            echo "❌ Template rendering failed with release name '$release'"
            exit 1
          fi
        done
        
        echo "✅ Template rendering tests passed"

    - name: YAML validation
      run: |
        chart_name=$(basename "${{ matrix.chart }}")
        echo "🔍 Validating YAML syntax for chart: ${{ matrix.chart }}"
        
        if ! helm template "validate-$chart_name" "${{ matrix.chart }}" --namespace validation | python3 -c "import sys, yaml; yaml.safe_load_all(sys.stdin.read())" > /dev/null 2>&1; then
          echo "❌ YAML syntax validation failed"
          exit 1
        fi
        
        echo "✅ YAML syntax validation passed"

    - name: Conditional features tests
      run: |
        chart_name=$(basename "${{ matrix.chart }}")
        echo "🧪 Testing conditional features for: ${{ matrix.chart }}"
        
        # Test default values produce output
        default_output=$(helm template "test-$chart_name" "${{ matrix.chart }}" --namespace test)
        if [ -z "$default_output" ]; then
          echo "❌ Chart produces no output with default values"
          exit 1
        fi
        
        # Test conditional features if they exist
        if grep -q "externalRouting" "${{ matrix.chart }}/values.yaml" 2>/dev/null; then
          echo "Testing externalRouting feature..."
          if ! helm template "test-external" "${{ matrix.chart }}" --set externalRouting.enabled=true --namespace test > /dev/null; then
            echo "❌ Failed with externalRouting enabled"
            exit 1
          fi
        fi
        
        if grep -q "autoscaling" "${{ matrix.chart }}/values.yaml" 2>/dev/null; then
          echo "Testing autoscaling feature..."
          if ! helm template "test-hpa" "${{ matrix.chart }}" --set autoscaling.enabled=true --namespace test > /dev/null; then
            echo "❌ Failed with autoscaling enabled"
            exit 1
          fi
        fi
        
        if grep -q "config.enabled" "${{ matrix.chart }}/values.yaml" 2>/dev/null; then
          echo "Testing config feature..."
          if ! helm template "test-config" "${{ matrix.chart }}" --set config.enabled=true --namespace test > /dev/null; then
            echo "❌ Failed with config enabled"
            exit 1
          fi
        fi
        
        if grep -q "secrets.enabled" "${{ matrix.chart }}/values.yaml" 2>/dev/null; then
          echo "Testing secrets feature..."
          if ! helm template "test-secrets" "${{ matrix.chart }}" --set secrets.enabled=true --namespace test > /dev/null; then
            echo "❌ Failed with secrets enabled"
            exit 1
          fi
        fi
        
        echo "✅ Conditional features tests passed"

    - name: Security check
      run: |
        echo "🔒 Checking security best practices for: ${{ matrix.chart }}"
        
        # Check for hardcoded namespaces
        namespace_issues=$(grep -r "namespace:" "${{ matrix.chart }}" --include="*.yaml" | grep -v "{{ .Release.Namespace }}" | grep -v "{{ .Values.*namespace" | grep -v "# " || true)
        if [ -n "$namespace_issues" ]; then
          echo "⚠️  Warning: Found potential hardcoded namespaces:"
          echo "$namespace_issues"
        fi
        
        # Generate template and check for security contexts
        template_output=$(helm template "security-test" "${{ matrix.chart }}" --namespace test)
        
        # Check for security context in deployments
        if echo "$template_output" | grep -q "kind: Deployment"; then
          if ! echo "$template_output" | grep -q "securityContext:"; then
            echo "⚠️  Warning: Deployment found but no securityContext defined"
          else
            echo "✅ Security context found in deployment"
          fi
        fi
        
        # Check for ServiceAccount
        if echo "$template_output" | grep -q "kind: ServiceAccount"; then
          echo "✅ ServiceAccount defined"
        else
          echo "⚠️  Info: No ServiceAccount defined (may use default)"
        fi
        
        echo "✅ Security check completed"
